make server.js, make routes. look at routes on api.js use those routes. use :id for +id look at post and get. Study the fetch calls. creates a routes folder and a models folder. Copy activites 15 server.js (dont take the routes and rename localhost to finess tracker and dont import models(we dont have any yet). Make the html routes.
 Look for office hours deployment in class activites for heroku deployment.

run npm run seed. Seed the database.

make routes,
make models,
upload to heroku,

look in express hot restaurant, use router instead of app. Make 2 gets, 4 routes, make html and api routes.

Make last html route called stats or just like html exercise route.